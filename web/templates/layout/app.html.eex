<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Busseur TriMet app">
    <meta name="author" content="Jon de la Motte">

    <title>Busseur</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>

    <!--
    <header class="pure-menu pure-menu-horizontal pure-menu-right">
        <a href="/" class="pure-menu-heading pure-menu-link">Busseur</a>
    </header>
    -->

    <div class="bs-container">
      <%= if get_flash(@conn, :info) do %>
        <div class="bs-well-info"><%= get_flash(@conn, :info) %></div>
      <% end %>

      <%= if get_flash(@conn, :error) do %>
        <div class="bs-well-error"><%= get_flash(@conn, :error) %></div>
      <% end %>

      <%= @inner %>
    </div>
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
